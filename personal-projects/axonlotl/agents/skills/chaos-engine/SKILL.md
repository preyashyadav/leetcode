# Skill: chaos-engine
Injects controlled failures to measure resilience and recovery behavior.
Rules: use bounded, reversible fault injections, record R and I_dev metrics per scenario, and never corrupt persisted state beyond the current run.

