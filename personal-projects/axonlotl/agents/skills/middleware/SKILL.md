# Skill: middleware
Applies interceptors around agent calls to observe and modify traffic.
Rules: implement before/modify/after hooks as pure functions, preserve request intent unless explicitly instructed, and log changes as structured JSON diffs.

